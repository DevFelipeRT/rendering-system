{{-- Loading Indicator with Custom Icon and Animation --}}
<div id="page-loader" class="page-loader">
    <div class="loader-content">
        {{-- Replaced the Bootstrap spinner with your custom icon --}}
        <i class="bi bi-code-square loader-icon" aria-hidden="true"></i>
        <p class="text-secondary mt-3 mb-0">Loading system...</p>
    </div>
</div>

{{-- Loading Indicator Scripts and Styles --}}
@once
    @push('styles')
        @css('partial/main/loading-indicator')
    @endpush
    @push('scripts')
        @js('partial/main/loading-indicator')
    @endpush
@endonce